# Nezha Agent 配置示例
# 复制此文件为 .env 并填写你的配置信息

# Nezha 服务器地址（必填）
# 格式：域名:端口 或 IP:端口
NZ_SERVER=your-server-address:port

# 是否启用 TLS（必填）
# true 或 false
NZ_TLS=true

# 客户端密钥（必填）
# 从 Nezha 面板获取
NZ_CLIENT_SECRET=your-secret-here

# 客户端唯一标识（可选）
# 如果不设置，Agent 会自动生成（重启后会变化）
# 建议：首次运行后查看日志获取 UUID，然后设置此值固定 UUID
# NZ_UUID=your-fixed-uuid-here

# 上报延迟（可选）
# 单位：秒，默认为 1
NZ_REPORT_DELAY=1

# 跳过连接检查（可选）
# true 或 false，默认为 false
NZ_SKIP_CONN=false

# 跳过进程检查（可选）
# true 或 false，默认为 false
NZ_SKIP_PROCS=false

# 禁用自动更新（可选）
# true 或 false，默认为 false（允许自动更新）
NZ_DISABLE_AUTO_UPDATE=false

# 禁用强制更新（可选）
# true 或 false，默认为 false（允许强制更新）
NZ_DISABLE_FORCE_UPDATE=false

# 禁用命令执行（可选）
# true 或 false，默认为 true
# 安全选项：禁用后无法通过面板执行命令
# 如果需要通过面板执行命令，请设置为 false
NZ_DISABLE_COMMAND_EXECUTE=true

# 禁用 NAT 穿透（可选）
# true 或 false，默认为 false
NZ_DISABLE_NAT=false

# 使用 IPv6（可选）
# true 或 false，默认为 false
NZ_USE_IPV6=false

# 启用 GPU 监控（可选）
# true 或 false，默认为 false
NZ_GPU=false

# 启用温度监控（可选）
# true 或 false，默认为 false
NZ_TEMPERATURE=false

# IP 上报周期（可选）
# 单位：秒，默认为 1800（30分钟）
NZ_IP_REPORT_PERIOD=1800

# 调试模式（可选）
# true 或 false，默认为 false
# 启用后会输出详细的调试日志
NZ_DEBUG=false

# 时区设置（可选）
# 默认为 Asia/Shanghai
TZ=Asia/Shanghai

